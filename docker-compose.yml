services:
  bot:
    image: leroideskiwis/omega-bot:v1.0.2
    volumes:
      - ./database.sqlite:/opt/omega-bot/database.sqlite
    restart: always
    build:
        context: .
        dockerfile: Dockerfile
